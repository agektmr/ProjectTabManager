import { ProjectState } from '../fragments/Project';

export const projects = (
  state: ProjectState[] = [],
  action
): ProjectState[] => {
  switch (action.type) {
    case '':
  }
  return state;
};
